{
  "subscriptionRouting": {
    "reservePercent": 20,
    "telemetryMaxAgeSeconds": 300,
    "cooldownSeconds": 1800
  },
  "rules": [
    {
      "when": "The task depends on fresh news, current events, live public facts, or recent market and product changes.",
      "use": { "harness": "grok" },
      "why": "Grok is the preferred dispatch when current web-connected context is central to the work."
    },
    {
      "when": "The task is a trivial mechanical edit such as a rote rename, formatting sweep, targeted typo fix, or simple file gathering.",
      "use": { "harness": "claude", "model": "haiku", "effort": "low" },
      "why": "Use the cheapest fast profile when the task is narrow and low ambiguity."
    },
    {
      "when": "The task should run on a Cursor-native model rather than a standalone vendor CLI.",
      "use": [
        { "harness": "cursor", "model": "cursor-grok-4.6-high", "quotaWindow": "auto_usage" }
      ],
      "why": "Cursor bills its included, auto, and API pools separately, so this route is priced on the pool it actually draws on instead of on whichever pool is emptiest. Confirm the window id against quota-axi --json before copying this."
    },
    {
      "when": "The task is standard coding work suited for Gemini or Claude models on Antigravity.",
      "use": [
        { "harness": "agy", "model": "gemini-3.7-flash-high", "effort": "high", "quotaWindow": "gemini_5h" }
      ],
      "why": "Antigravity bills Gemini and Claude/GPT pools separately; quotaWindow prices on the Gemini pool. Confirm both the model id against agy models (or bin/fm-model-refresh.sh) and the window id against quota-axi --json before copying this, because a stale model id surfaces only as an agy trust-gate timeout at launch."
    },
    {
      "when": "The task is a big or ambiguous multi-file feature, a risky refactor, or work that requires holding many moving parts in mind.",
      "use": [
        { "harness": "claude", "provider": "claude", "model": "claude-sonnet-5", "effort": "high" },
        { "harness": "codex", "provider": "codex", "model": "gpt-5.5", "effort": "high" }
      ],
      "why": "Use comparable strong coding profiles while fresh capacity evidence and the configured reserve permit them."
    },
    {
      "when": "The captain asks for OpenRouter Auto Router, or remaining native seats are empty and OpenRouter should pick the model for this turn.",
      "use": { "harness": "opencode", "model": "openrouter/auto" },
      "why": "OpenRouter Auto Router is the model id openrouter/auto. Firstmate forwards that slug as --model on opencode. There is no extra Auto Router fee; you pay the selected model. LiteLLM catalogs may expose the same router as or-auto."
    }
  ],
  "default": [
    { "harness": "claude", "provider": "claude", "model": "sonnet", "effort": "medium" },
    { "harness": "codex", "provider": "codex", "model": "gpt-5.5", "effort": "medium" }
  ],
  "modelFallback": {
    "agy": ["gemini-3.7-flash-high", "gemini-3.6-flash-high", "gemini-3.5-flash-high", "claude-sonnet-4-6", "claude-opus-4-6-thinking"],
    "cursor": ["cursor-grok-4.6-high", "auto"],
    "opencode": ["openrouter/auto", "x-preview-f-free", "big-pickle", "hy3-free"],
    "cline": ["glm-5.3", "kimi-k3"],
    "claude": ["claude-sonnet-5", "sonnet", "haiku"]
  },
  "modelFallbackCycles": ["cline"],
  "fallbackLanes": ["codex", "agy", "cursor", "opencode", "cline"]
}
